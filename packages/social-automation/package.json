{
  "name": "@inseries/social-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Content-automation scaffolding for future social network posting (Instagram first). No real network calls, no real AI generation — infrastructure only.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5",
    "tsx": "^4"
  }
}
